# Firmware driver for the delay stage contents

- main.py holds the final implementation of the firmware driver
- test.py holds functions for testing functionality
- stage_lib holds the library defining the firmware
